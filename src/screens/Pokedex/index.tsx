import {SafeAreaView, Text} from 'react-native';
import React from 'react';

type Props = {};

const Pokedex = ({}: Props) => {
  return (
    <SafeAreaView>
      <Text>Pokedex</Text>
    </SafeAreaView>
  );
};

export default Pokedex;
